import math
velocidade = float(input("Velocidade média do veículo?: "))
distancia = float(input("Distância à Percorrer?: "))
resultado = distancia / velocidade

print("Tempo de viagem de",resultado,"horas")